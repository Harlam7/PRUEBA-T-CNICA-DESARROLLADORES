// cuenta.service.ts
/*
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class CuentaService {
  private baseUrl = 'http://localhost:8080/api/cuentas';  // Reemplaza con la URL real del backend

  constructor(private http: HttpClient) {}

  consultarSaldo(cuentaId: number): Observable<number> {
    return this.http.get<number>(`${this.baseUrl}/${cuentaId}/saldo`);
  }

  // Otros métodos para interactuar con el backend
}
*/
